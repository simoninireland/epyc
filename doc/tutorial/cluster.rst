.. _cluster-experiment:

.. currentmodule:: epyc

Running on a cluster
--------------------

So far so good: we can run an experiment at point in a parameter space. But a lot of the time those experiments will be
individually time-consuming, so running a large number of them will be
